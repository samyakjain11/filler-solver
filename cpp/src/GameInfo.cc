#include "GameInfo.h"

GameInfo::GameInfo(const std::string& opponentColor, const std::string& myColor, int opponentScore, int myScore)
    : opponentColor(opponentColor), myColor(myColor), opponentScore(opponentScore), myScore(myScore) {
    // Constructor implementation
}
// Implement any additional methods for GameInfo here
